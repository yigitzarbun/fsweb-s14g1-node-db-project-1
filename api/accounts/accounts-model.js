const getAll = () => {
  // KODLAR BURAYA
}

const getById = id => {
  // KODLAR BURAYA
}

const create = account => {
  // KODLAR BURAYA
}

const updateById = (id, account) => {
  // KODLAR BURAYA
}

const deleteById = id => {
  // KODLAR BURAYA
}

module.exports = {
  getAll,
  getById,
  create,
  updateById,
  deleteById,
}
